import type { CapacitorConfig } from '@capacitor/cli';


const config: CapacitorConfig = {
	appId  : 'com.example.app',
	appName: 'angular-multi-locale-ssr-capacitor',
	webDir : 'dist/browser'
};

export default config;
